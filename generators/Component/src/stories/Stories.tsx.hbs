import { Meta, StoryObj } from "@storybook/react";

import { My{{pascalCase name}} } from "../{{pascalCase name}}";

const meta: Meta<typeof My{{pascalCase name}}> = {
    title: "Components/My{{pascalCase name}}",
    component: My{{pascalCase name}},
    argTypes: {
        role: {
            control: { type: "text" },
        }
    }
}

export default meta;
type Story = StoryObj<typeof  My{{pascalCase name}}>;

export const Default: Story = {
    args: {
        role: "test"
    }
};